services:
  aep:
    build: .
    ports:
      - "8000:8000"
    environment:
      - AEP_DATABASE_URL=sqlite:///E:/Agent-Execution-Partnership/AI-Memory/memory.db
    command: ["aep", "serve", "--host", "0.0.0.0", "--port", "8000"]
